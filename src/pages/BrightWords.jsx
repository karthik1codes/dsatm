import React from 'react'
import Home from './Home'

// BrightWords now simply reuses the main Home experience so navigation stays within the SPA.
function BrightWords() {
  return <Home />
}

export default BrightWords

